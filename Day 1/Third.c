main()
{
	int n = 100;
	
	if (n > 50) {
		printf("%d bigger than 50. \n", n);
	}else{
		printf("%d not bigger than 50. \n", n);
	}
	
	
	n = 20;
	
	if (n > 50) {
		printf("%d bigger than 50. \n", n);
	}else{
		printf("%d not bigger than 50. \n", n);
	}
	
    return 0;
}
